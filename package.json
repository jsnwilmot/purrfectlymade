{
  "name": "purrfectlymade-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
